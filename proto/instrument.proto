syntax = "proto3";

package instrument;

message Empty {}

message Event {
  bytes encoded = 1;
}

message Command {
  bytes payload = 1;
}

message CommandResponse {
  bytes payload = 1;
}

service Instrument {
  rpc NotifyMe(Empty) returns (stream Event) {};
  rpc Send(Command) returns (CommandResponse) {};
}

