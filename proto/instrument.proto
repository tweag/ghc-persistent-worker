syntax = "proto3";

package instrument;

message Empty {}

message Encoded {
  bytes payload = 1;
}

service Instrument {
  rpc Events(Empty) returns (stream Encoded) {};
  rpc Api(Encoded) returns (Encoded) {};
}

